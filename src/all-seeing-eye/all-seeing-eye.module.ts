import { Module } from '@nestjs/common';

// AllSeeingEye entity: passive oversight that observes, logs, compares, and signals via an immutable ledger. Spec: docs/specs/AST_AllSeeingEye_AGENT_EN.md
@Module({})
export class AllSeeingEyeModule { }
