import { Module } from '@nestjs/common';

// Commission entity: accrues fees into an epoch pool and distributes them post-factum by node weight. Spec: docs/specs/AST_Commission_AGENT_EN.md
@Module({})
export class CommissionModule { }
