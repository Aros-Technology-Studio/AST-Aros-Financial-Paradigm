import { Module } from '@nestjs/common';

// Emission entity: mints on PoT-verified processes and burns the process part on completion. Spec: docs/specs/AST_Emission_AGENT_EN.md
@Module({})
export class EmissionModule { }
