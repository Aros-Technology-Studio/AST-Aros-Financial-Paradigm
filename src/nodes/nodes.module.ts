import { Module } from '@nestjs/common';

// Nodes entity: registers nodes and tracks influence from work and reputation, with post-factum payment and retention. Spec: docs/specs/AST_Nodes_AGENT_EN.md
@Module({})
export class NodesModule { }
