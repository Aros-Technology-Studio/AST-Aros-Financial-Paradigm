import { Module } from '@nestjs/common';

// ProcessOrchestrator entity: wires all entities into the full Model-1 process lifecycle. Spec: docs/specs/AST_Ontology_FULL_AGENT_EN.md
@Module({})
export class OrchestratorModule { }
