import { Module } from '@nestjs/common';

// PoT (Proof of Transaction) entity: verifies composite criteria into a binary verdict and authorizes emission. Spec: docs/specs/AST_PoT_AGENT_EN.md
@Module({})
export class PotModule { }
