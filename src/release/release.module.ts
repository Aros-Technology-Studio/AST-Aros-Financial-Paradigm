import { Module } from '@nestjs/common';

// Release entity: evaluates the release condition (reserve index above threshold and velocity above target). Spec: docs/specs/AST_Release_AGENT_EN.md
@Module({})
export class ReleaseModule { }
