import { Module } from '@nestjs/common';

// Reserve entity: derives the reserve index from confirmed transaction volume. Spec: docs/specs/AST_Reserve_AGENT_EN.md
@Module({})
export class ReserveModule { }
